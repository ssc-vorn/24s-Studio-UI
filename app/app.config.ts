export default defineAppConfig({
  ui: {
    colors: {
      primary: 'primary',
      neutral: 'slate'
    },
    button: {
      slots: {
        base: 'font-medium rounded-none'
      }
    }
  }
})
